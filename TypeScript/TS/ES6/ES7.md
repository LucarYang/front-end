* Array.prototype.includes includes方法用来检测是否存在某元素，返回布尔值
* 指数操作符 ** 用来表示幂运算，功能与Math.pow结果相同
```js
const a = ['小新', '正南', '阿呆', , '小彻']
console.log(a.includes('妮妮')) //false

console.log(2 ** 10)//1024
console.log(Math.pow(2, 10))//1024
```